function animateAttack(player) {
    // Здесь можно добавить логику для анимации атаки
    console.log(`${player.name} attacks!`);
}

function animateDamage(player) {
    // Анимация получения урона
    console.log(`${player.name} takes damage!`);
}